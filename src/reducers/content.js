const content = (state = [], action) => {
  switch (action.type) {
    case 'ADD_ROW':
      return [

      ];
    case 'MOVE_ROW':
      return [

      ];
    default:
      return state;
  }
};

export default content;